# Environment Configuration for Platform MCP Server

# Server Configuration
PORT=8080
NODE_ENV=development

# Microsoft Entra ID (Azure AD) Configuration
# Option 1: Use MS Entra specific environment variables (recommended)
MS_ENTRA_TENANT_ID=your-tenant-id-here
MS_ENTRA_CLIENT_ID=your-client-id-here

# Option 2: Use generic auth configuration (alternative)
# AUTH_JWKS_URI=https://login.microsoftonline.com/{tenant-id}/discovery/v2.0/keys
# AUTH_ISSUER=https://login.microsoftonline.com/{tenant-id}/v2.0
# AUTH_AUDIENCE=your-client-id-here
# AUTH_TENANT_ID=your-tenant-id-here

# Authentication Configuration
AUTH_CACHE_TTL=600
AUTH_ROLE_CLAIM_PATH=roles
# For groups instead of roles, use: AUTH_ROLE_CLAIM_PATH=groups

# Permission Mapping (JSON string)
# Maps roles/groups to MCP permissions
AUTH_PERMISSION_MAPPING={"admin":["*"],"developer":["k8s:view","k8s:restart"],"viewer":["k8s:view"]}

# CORS Configuration
CORS_ORIGINS=http://localhost:3000,https://your-frontend-domain.com

# Kubernetes Configuration (if needed)
# KUBECONFIG=/path/to/kubeconfig

# Logging
LOG_LEVEL=info
